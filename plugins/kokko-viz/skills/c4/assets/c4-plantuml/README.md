# C4-PlantUML (vendored)

The four library files `/c4-map` copies into `codemap/.c4-plantuml/`, bundled
here so map generation needs no network access.

- Source: <https://github.com/plantuml-stdlib/C4-PlantUML> (`master` branch)
- License: MIT (see the header of each `.puml` file)

Kept byte-identical to upstream — pre-commit hooks are excluded from this
directory on purpose. To refresh, re-download the four files from the source
repository.
